services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "5000:5000"
    volumes:
      - ./:/app

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: buffet_shao
      POSTGRES_USER: buffet_shao
      POSTGRES_PASSWORD: buffet_shao
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U buffet_shao"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
